## 场景查询问题排查计划

### 问题分析
根据代码分析和数据库检查，场景work12的4G和5G小区在15分钟表中都有数据（2025-12-28 15:00:00），但查询不到数据。

### 排查步骤

#### 1. 前端参数验证
- **检查时间范围**：确认前端实际发送的时间参数是否为2025-12-28 14:00:00至2025-12-28 16:00:00
- **检查场景选择**：确认是否正确选择了"work12"场景
- **检查网络类型**：确认是否选择了"混查"模式
- **检查粒度**：确认是否选择了"15分钟"粒度

#### 2. 后端时间处理逻辑检查
- **检查`latest_ts`值**：验证`scenario_service.latest_time()`返回的最新数据时间
- **检查`parse_time_range`函数**：分析时间范围处理逻辑，确认是否正确处理用户输入的时间
- **检查时间参数传递**：确认前端时间参数是否正确传递到后端

#### 3. 场景查询逻辑检查
- **检查场景小区获取**：验证`scenario_service.list_cells()`是否正确返回场景的小区列表
- **检查网络类型识别**：验证从CGI推断网络类型的逻辑是否正确
- **检查SQL查询生成**：分析生成的SQL查询语句是否正确

#### 4. 数据库查询验证
- **直接执行SQL**：使用代码生成的SQL语句直接查询数据库
- **检查时间格式**：确认数据库中时间存储格式与查询格式一致
- **检查索引使用**：确认时间字段是否使用索引加速查询

### 预期修复点
1. **时间范围处理**：可能需要修改`parse_time_range`函数，确保正确处理用户指定的历史时间范围
2. **场景查询优化**：可能需要优化场景小区数据的获取和处理逻辑
3. **前端参数验证**：可能需要在前端添加时间范围有效性检查

### 验证方法
使用以下参数测试：
- 时间范围：2025-12-28 14:00:00 至 2025-12-28 16:00:00
- 粒度：15分钟
- 查询类型：小区
- 场景：work12
- 网络类型：混查

预期结果：返回场景work12的4G和5G小区在2025-12-28 15:00:00的数据。